/** Mirrors Blazor AlertSeverity (CSS modifiers: info/success/warning/danger). */
export type AlertSeverity = 'info' | 'success' | 'warning' | 'danger';

/** Mirrors Blazor LoaderSize. */
export type LoaderSize = 'small' | 'medium' | 'large';

/** Mirrors Blazor ToastPosition (CSS modifiers). */
export type ToastPosition =
  | 'top-right'
  | 'top-left'
  | 'bottom-right'
  | 'bottom-left'
  | 'top-center'
  | 'bottom-center';
